import React from 'react';
import {StyleSheet, View} from 'react-native';

interface CommonArtCardProps {}

const CommonArtCard = ({}: CommonArtCardProps) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default CommonArtCard;
