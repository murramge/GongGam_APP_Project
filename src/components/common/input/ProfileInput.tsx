import React from 'react';
import {StyleSheet, View} from 'react-native';

interface ProfileInputProps {}

const ProfileInput = ({}: ProfileInputProps) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default ProfileInput;
